journalctl -b | grep gedit -> syslog do sistema sobre a aplicacao
dmesg -> mensgens captadas do kernel
ps aux | grep "appname"
echo 1 | sudo tee /proc/[pid]/make-it-fail

sudo trace-cmd record -e function -P <PID>
sudo trace-cmd report | grep 5344

pidof firefox
 echo 1024 > /sys/kernel/debug/tracing/buffer_size_kb // buffer de quantidade de bytes que quero no trace, pega os ultimos
sudo code --no-sandbox --user-data-dir=/tmp/vscode-root-data sched.h



